return (
  <div className="flex items-center justify-center h-screen flex-col">
    <Card className="w-80 h-auto flex items-center my-5">
      <p className="mt-5">Don't have an account? Sign up!</p>

      <div className="my-5">
        <div className="flex items-center justify-center">
          <Avatar
            icon={<AvatarIcon />}
            classNames={{
              base: "bg-gradient-to-br from-[#FFB457] to-[#FF705B]",
              icon: "text-black/80",
            }}
          />
        </div>
        {/* Input Name */}
        <div className="my-3">
          <Input
            isRequired
            size="sm"
            label="First Name "
            variant="bordered"
            placeholder="Enter your name"></Input>
        </div>
        <div className="my-3">
          <Input
            isRequired
            size="sm"
            label="Last Name"
            variant="bordered"
            placeholder="Enter your name"></Input>
        </div>
        {/* Input Email */}
        <div className="my-3">
          <Input
            isRequired
            size="sm"
            label="Email"
            variant="bordered"
            placeholder="Enter your email"></Input>
        </div>
        {/* Input PassWord */}
        <div className="my-3">
          <Input
            isRequired
            size="sm"
            label="Password"
            variant="bordered"
            placeholder="Enter your password"
            endContent={
              <button
                className="focus:outline-none"
                type="button"
                onClick={toggleVisibility}
                aria-label="toggle password visibility">
                {isVisible ? (
                  <EyeSlashFilledIcon className="text-2xl text-default-400 pointer-events-none" />
                ) : (
                  <EyeFilledIcon className="text-2xl text-default-400 pointer-events-none" />
                )}
              </button>
            }
            type={isVisible ? "text" : "password"}
            className="max-w-xs"
          />
        </div>
      </div>
      <Button className="w-40 mb-5" size="md">
        Create Account
      </Button>
    </Card>
    <p className="text-xs">
      Already have an account?{" "}
      <Link to="/" className="text-blue-500 hover:underline no-underline font-semibold">
        Log In
      </Link>
    </p>
  </div>
);





// Codigo Chatgpt

return (
  <div className="flex items-center justify-center h-screen flex-col px-4 md:px-0">
    <Card className="w-full max-w-xs sm:max-w-md lg:max-w-lg h-auto flex items-center my-5 p-4">
      <p className="mt-5 text-center text-sm sm:text-base md:text-lg">
        Don't have an account? Sign up!
      </p>

      <div className="my-5 w-full">
        {/* Avatar */}
        <div className="flex items-center justify-center">
          <Avatar
            icon={<AvatarIcon />}
            classNames={{
              base: "bg-gradient-to-br from-[#FFB457] to-[#FF705B]",
              icon: "text-black/80",
            }}
          />
        </div>
        {/* Input Fields */}
        <div className="my-3 w-full">
          <Input
            isRequired
            size="sm"
            label="First Name"
            variant="bordered"
            placeholder="Enter your name"
          />
        </div>
        <div className="my-3 w-full">
          <Input
            isRequired
            size="sm"
            label="Last Name"
            variant="bordered"
            placeholder="Enter your name"
          />
        </div>
        <div className="my-3 w-full">
          <Input
            isRequired
            size="sm"
            label="Email"
            variant="bordered"
            placeholder="Enter your email"
          />
        </div>
        <div className="my-3 w-full">
          <Input
            isRequired
            size="sm"
            label="Password"
            variant="bordered"
            placeholder="Enter your password"
            endContent={
              <button
                className="focus:outline-none"
                type="button"
                onClick={toggleVisibility}
                aria-label="toggle password visibility">
                {isVisible ? (
                  <EyeSlashFilledIcon className="text-2xl text-default-400 pointer-events-none" />
                ) : (
                  <EyeFilledIcon className="text-2xl text-default-400 pointer-events-none" />
                )}
              </button>
            }
            type={isVisible ? "text" : "password"}
          />
        </div>
      </div>

      {/* Botón adaptable */}
      <Button className="w-full " size="md">
        Create Account
      </Button>
    </Card>
    <p className="text-xs text-center mt-4">
      Already have an account?{" "}
      <Link to="/" className="text-blue-500 hover:underline font-semibold">
        Log In
      </Link>
    </p>
  </div>
);